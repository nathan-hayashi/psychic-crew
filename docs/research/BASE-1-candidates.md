# BASE-1 — the candidate set, named; the axes, reaffirmed frozen

Arc 3 opens the way C-18 demands: the measuring stick is fixed BEFORE anything is measured.

## The reuse law, first

CrewAI is DONE. STRESS-1's five-axis comparison (its report §E, verdict 4–1 with the
segmentation frame stated) is REUSED, not re-frozen — the row stays comparable with every
future candidate precisely because the axes do not move. Nothing in this arc re-opens it.

## The five axes, reaffirmed VERBATIM (frozen at RSCH-2; C-18)

Axis law, quoted: **"verifiable outcome, not velocity."** The axes, as the STRESS-1 report
carries them:

1. **Setup time**
2. **Control surface**
3. **Verification depth**
4. **Evidence trail**
5. **Human-gate ergonomics**

These five, in this order, with this law — BASE-2's matrix binds 4 candidate rows × exactly
these axes, verdict vocabulary {OURS, THEIRS, TIE, NOT-COMPARABLE-DOCUMENTED-ONLY}, roll-up
agreement asserted.

## The posture, stated before the candidates

None of the four is on disk as a corpus, and HC-5 bars fetching. Every fact below is
DOCUMENTED-ONLY, carried from RSCH-2's dated triage (2026-08-26, evidence class [Ekn] as
recorded there). No measured comparison is possible without an operator-provided snapshot —
which is exactly why BASE-2's vocabulary contains NOT-COMPARABLE-DOCUMENTED-ONLY: the honest
cell for an axis that documentation alone cannot settle.

## The four candidates

### 1. Dify

LLMOps / AI-app builder. License AS DOCUMENTED: **"OSS (partly)"** — the one candidate whose
license needs care before any deeper engagement; the partly-open posture is itself a
comparison datum (this estate is MIT end to end). Comparison value: a visual agent/app builder
— the UX pole opposite this estate's file-and-law pole; strongest on the Setup-time axis,
weakest documentable on Evidence trail.

### 2. Langflow

Visual LangChain flow builder. License AS DOCUMENTED: MIT. Comparison value: the visual-flow
UX reference RSCH-2 already tied to Sidekick's fill-in-fields front end — the natural
Setup-time and ergonomics comparator for the ONE estate surface that is a UI.

### 3. MetaGPT

SOP-driven multi-agent "software company." License AS DOCUMENTED: MIT. Comparison value: its
SOP-encoded-as-agent-roles is the closest philosophical neighbor to this estate's
plan-as-contract — the Control-surface axis comparison writes itself; Verification depth as
documented is developer-authored, the same segmentation CrewAI showed.

### 4. AutoGen

Conversational multi-agent framework (also LEARNING-SHELF in the triage). License AS
DOCUMENTED: MIT. Comparison value: async multi-agent messaging vs this estate's
arbiter-brokered synchronous custody — the Evidence-trail axis comparison is the informative
one (message logs vs release law).

## What BASE-2 owes, bound now

`docs/research/BASE-2-comparison.md`: exactly 4 candidate rows × the 5 frozen axes; verdict
vocabulary as above; a roll-up row whose totals must agree with the cells; the honesty frame
first-class (the STRESS-1 segmentation lesson quoted, not paraphrased); Dify's license nuance
carried into any THEIRS/OURS call on that row. No axis may be added, dropped, or reworded —
that is what frozen means.
