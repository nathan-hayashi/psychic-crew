# STUB-1 — the skill-surface drift audit (honest verdict: NO-OP)

**Motivating incident class:** the retired orchestration guide's rot — documentation carrying
pasted command output that ages into confident falsehood. The orca TAKE (discovery-stub
anti-drift) mechanizes current-docs-never-memory; this audit asks where the estate's skill
surface carries that risk, and answers with evidence.

**Census basis, declared:** `git ls-files '.claude/skills/*/SKILL.md'` — TRACKED files only,
never a filesystem glob (the gitignored reference corpora carry ~30 third-party SKILL.md
files a glob would sweep in; the review named this trap). Sibling surfaces are audited as
announced conditional legs, not bound rows.

## The audit table (parent, bound both ways)

```text
# STUB-AUDIT v1
.claude/skills/intake/SKILL.md	CLEAN	prose-first contract skill; classifier is a versioned data fence, no pasted output
.claude/skills/army-selector/SKILL.md	CLEAN	prose + ARMY-TABLE data fence; no terminal transcripts, no version-pinned output
.claude/skills/threshold-router/SKILL.md	CLEAN	pure procedure prose; no command output claimed at all
```

## Sibling surfaces (announced, conditional)

- psychic-plugins `skills/{gate-machine, request-contract, unknown-audit}` — CLEAN by prior
  law: the S1 zero-CLI-content arm already forbids executable shapes in shipped bodies, a
  stricter property than freedom-from-pasted-transcripts.
- psychic-crew-lite `.claude/skills/packs/confluence-docs` — CLEAN: templates and standards
  prose; its attack fixtures deliberately carry hostile shapes and are fixtures, not skills.
- psychic-templates / psychic-sidekick / psychic-repurpose — carry no skill surface; recorded,
  not silently skipped.

## The one mechanical ratchet

No tracked parent SKILL.md may carry a pasted-terminal-transcript shape: a fenced block whose
line opens `$ ` with a semver-shaped token nearby. Expected census 0, zero-forever, fire-probed
with a planted transcript every run. Versioned DATA fences (`# X v1` headers) are deliberately
unmatched — version-labels-as-data are the house pattern, not rot.

## The verdict

**NO-OP** — every audited surface is already prose-first and transcript-free. Recorded as a
finding, not padded into work: the estate's skills comply because prose-first was the law from
their birth gates, and the ratchet now keeps it that way mechanically.
