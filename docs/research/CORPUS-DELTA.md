# CORPUS-DELTA — four refresh docs, one gate; arc 2's coverage arithmetic closes

**The tier, stated before the content:** these four census rows are ETL-BUILD — consumed at
build time under §11 — and the coverage table binds them to THIS gate at tier `delta`, which
the table defines as refresh, not dive. Under HC-5 nothing is fetched: "refresh" means the
ON-DISK SNAPSHOT re-swept against what the build consumed, with a verdict on whether the
snapshot holds actionable material beyond what was taken. Depth is listing-and-README level BY
DECLARED TIER, not by omission. Sidecars excluded throughout.

---

## 1. turbo

**Identity:** "A composable dev process for agentic coding harnesses, packaged as modular
skills," with sibling Claude Code and Codex editions (Codex experimental, parity but less
use). **Consumed at build:** the skills-pipeline shape that the operator's user-scope review
pipeline (plan-style → review-plan → evaluate → apply-findings → finalize) descends from; the
threshold-router still names turbo's finalize by name. **Delta verdict:** the Codex edition is
outside HC-7 and irrelevant here; the Claude edition's skill set is already mirrored in
spirit by the user-scope pipeline. NO ACTION — nothing in the snapshot exceeds what runs in
this environment today.

## 2. open-code-review

**Identity:** an open review-orchestration project (multi-reviewer flows over diffs).
**Consumed at build:** the multi-reviewer review shape — this estate runs it as
security-reviewer + quality-reviewer through the arbiter, and STRESS-1 benched the whole
release pipeline against exactly this class of flow. **Delta verdict:** the estate's form is
narrower and law-bound (findings schema, release separation, C-12); the snapshot offers
breadth (more reviewer roles) the zero-dispatch default deliberately declines. NO ACTION.

## 3. neatcontext-plugins

**Identity:** context-hygiene plugins (keeping working context small and clean) under CI.
**Consumed at build:** the context-discipline ideas that HC-8 formalized harder — disk
canonical, distill at gates, re-ground on session start; the plugin form also fed SIDE-2's
plugin-surface survey. **Delta verdict:** our discipline is stricter than the plugins' (law +
suites vs convention); the plugin packaging lessons were already taken at SIDE-2/S1.
NO ACTION.

## 4. mermaid-hybrid-stack-guide

**Identity:** the Mermaid hybrid-stack integration guide (WSL2/macOS/Linux platform matrix).
**Consumed at build:** FULLY — F6 transformed its 11 documented errors into executable
assertions (the 23-error corpus: 12 orchestration-guide + 11 mermaid-guide), and its platform
matrix anticipates the PORTABILITY work HARNESS-1 later did. **Delta verdict:** consumption
was total at F6; the snapshot holds no untransformed residue. NO ACTION — and this row is the
cleanest illustration of what ETL-BUILD means.

---

## The arithmetic, closed

With this gate every coverage row is discharged: 8 full-dive rows (7 single + the split pair),
4 delta rows (this document), 3 promoted (CORPUS-0), 2 done (RSCH-4, F6), 1 prohibited-exempt
(asserted by absence every suite run). 18 of 18 — the table that was bound at CORPUS-0 closes
exactly as its arithmetic said it must, and the census/coverage/questions arms hold it there
mechanically.
