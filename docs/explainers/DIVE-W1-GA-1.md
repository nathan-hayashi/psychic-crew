# DIVE-W1-GA-1, explained plainly

## What this gate is

The wave's third dive: gastown, re-opened under its new question — the S6 dive recorded their
three heartbeat stores and watchdog chain as concepts; this one asks for the MINIMAL
liveness/stall vocabulary this estate could adopt without adopting propulsion, and whether
NDI (nondeterministic idempotence) deserves a named place in our doctrine. The token
authorizes the reading. Prediction on record: TAKE-FEW.

## What the token buys

One dive doc with a measured read manifest, validator-bound verdicts, landing shapes, and the
register/queue/map moved atomically.

## What could break, and what catches it

The SOURCE-MAP-1 validator, proven firing on a planted fixture.

## What was found (the close)

Two takes: the two-plane liveness rule (agents report their own state; the observer makes
exactly one inference — freshness; absent signals never verdict; grace before first-signal
judgment) and the typed stall-classification vocabulary (nine detection REASONS, distinct
from lifecycle states, with downstream verdicts derived from the type). Both land as one
STALL-VOCAB shape beside the ARC4-1 earmarks. Two validations where gastown independently
arrived at our laws (self-stuck escalates rather than restarts; TOCTOU re-check before
action). And NDI, judged on its read definition, is DECLINED a name here — eventual
completion contradicts the gate law; what our FALLBACK protocol gropes toward already has
names. Outcome 2 — TAKE-FEW held, three predictions for three. The NDI register row closes
by decision; the stall row stays honestly OPEN until something ships.
