# DIVE-W1-HC-1, explained plainly

## What this gate is

The platform-currency sweep: have the facts this estate depends on drifted — the hook-event
set, PreCompact/PostCompact semantics (WORKAROUND-01's removal condition), model-fallback
behavior, the 1M-context variant behind OQ-2, and the Team-plan content-endpoint coverage
SIDE-5 flagged? Prediction on record: TAKE-FEW.

## What the token buys

Dated read-only fetches, a dive doc the validator binds, and the register rows those facts
carry moved to their honest states.

## What could break, and what catches it

The SOURCE-MAP-1 validator, proven firing on a planted fixture.
