# SIDE-PAGES-1 (parent record), explained plainly

The register's parked name got its scope and shipped: the sidekick form is now a hosted web
page (GitHub Pages, dev branch root, byte-identical to the local file because no build step
exists). The permission surface was widened by exactly one narrow line — the pages endpoint of
that single public repo — declared in-gate with its blast radius and containment per the
secrets contract. The sidekick suite verifies the lane forever in three honest states; the
parent records the program's last gate here. See the sidekick's own explainer for the working
detail.
