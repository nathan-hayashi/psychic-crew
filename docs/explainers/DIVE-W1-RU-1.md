# DIVE-W1-RU-1, explained plainly

## What this gate is

The wave's second dive: ruflo, re-opened under its new question — what would a parent-side
temporal-bisect layer actually need from their three-layer verification design (the S6 dive
recorded the concept; this one specifies the mechanism), and what does their implementation
get wrong that ours must not copy? The token authorizes the reading. Prediction on record:
TAKE-FEW.

## What the token buys

One dive doc with a measured read manifest, verdicts the generic validator binds, landing
shapes for takes, and the register/queue/map moved atomically.

## What could break, and what catches it

The SOURCE-MAP-1 validator, proven firing on a planted fixture.

## What was found (the close)

Two takes that CONVERGE with the orca dive's one: ruflo's per-platform JSONL history + pure
query tool, and their drift-vs-regression/flap-timeline semantics — together with orca's
evidence-run ledger they specify a single successor record (SUITE-ATTEST-1) that would close
the attestation gap and the temporal-bisect gap at once, per platform, at run-level
granularity. One REJECT with teeth: their Ed25519 signature derives its private key from the
public git commit — authentication theater our estate must never copy, and independent
confirmation that the babysitter-era decision (signing waits for an operator-held key) was
right. Outcome 2 TAKE-class — the TAKE-FEW prediction held. No register flips: the gaps stay
honestly OPEN until something ships.
