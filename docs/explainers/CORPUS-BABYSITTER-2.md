# CORPUS-BABYSITTER-2, explained plainly

## What changed

The intervention half, and the ladder's close. Babysitter intervenes with feedback-carrying
rejection loops (already our native arbiter→fixer flow), progressively rising quality targets
(rejected — our bar is fixed and iteration unbounded until green), durable out-of-chat
approvals (the network-full sibling of our zero-network remote lane — same problem, inverse
transport, recorded as independent validation), and cryptographically signed approvals — which
collide with the secrets contract: no key exists in this estate until a gate grants one, so
tamper-evidence is honestly BLOCKED, with the operator-held git-signing route recorded as the
one R-SEC-1-compatible option, on the operator's word only.

All eight full dives are now discharged; the census row flipped, the last question left the
table, and the questions arm reads 0 == 0 — empty-legal because the arm was made dynamic at
the first dive.

## Verify it yourself

```
./scripts/check-decision-matrices.sh | grep 'QUEUED row'    # 0 == 0
grep -A10 'The answer' docs/research/CORPUS-BABYSITTER-2.md
```

## What could break, and what catches it

The coverage table still binds every row: the four delta rows point at CORPUS-DELTA, and the
arc cannot close without it — the arithmetic that was fixed at CORPUS-0 does the holding.
