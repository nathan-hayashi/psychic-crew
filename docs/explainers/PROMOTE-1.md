# PROMOTE-1, explained plainly

## What changed

The operator read the announce planes' first live evidence and promoted exactly what the
evidence supports. The INTEGRITY plane hardened: from the promotion moment, a specialist
death without arbiter coverage is a hard FAIL — armed at a moment with zero grandfathers
(34 stops delivered proves the trail; 0 post-cut specialist stops means nothing historical
can red). The OBSERVATIONAL planes were re-affirmed announce-forever, with the reasons now
written in the arms themselves: pairing punishes killed and backgrounded agents; stall
candidates punish long-running sessions; liveness is evidence, not violation. The registry
gained its demotion policy in writing: maturity re-derives every run, so a rotted control
auto-demotes mechanically — always true, now stated. History is never graded retroactively;
append-only trails cannot be repaired, so only the future carries the hard plane.

## Verify it yourself

```
./scripts/run-crew-tests.sh all | grep -A6 'PROMOTE-1'
./scripts/validate-crew.sh | grep 'C-25b'
```

## What could break, and what catches it

A post-promo specialist death slipping coverage — the promoted FAIL branch, its firing
proven every run on a planted fixture death. The cut leaking history — the second control.
The promo constant or re-affirmation wording vanishing — needle arms.
